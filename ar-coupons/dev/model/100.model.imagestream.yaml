---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: arc-model
  labels:
    app.kubernetes.io/name: arc-model
    app.kubernetes.io/part-of: arc-dev
spec:
  lookupPolicy:
    local: true